# 定期試験対策QB エージェントルーター

このファイルは作業の振り分けだけを行う。詳細規則をここへ複製しない。

## 正本

- コード：GitHub `ykmz2000/wakan-qb-7k3m`
- 問題・解答・解説・修飾・出題履歴・添付状態：Supabase `qebvqcubtyfgaakrzbzh`
- 運用ルール、講義資料、過去問原資料：Google Drive「定期テスト対策QB 運用インデックス」配下

過去の完了報告や会話中の件数を正本として扱わない。今回触るシステムの最新状態を取得する。

## 作業別ルート

1. 最初にGoogle Driveの `00_最初に読む_運用インデックス` を確認する。
2. 内容整備は `01`・`02`、Supabase更新と保存後監査は `03`、資料・画像・PDFは `04` を必要な範囲だけ読む。
3. 新しい指摘を受けた場合だけ `05` の関連項目を検索し、UI・UX開発だけ `06` を読む。
4. 疾患知見が必要な場合だけ `07` を疾患名・症候・検査・治療・横断論点で検索する。全文を常時読み込まない。

## 境界

- UI・UXだけの依頼では、問題文、選択肢、解答、解説、修飾範囲を変更しない。
- 内容整備だけの依頼では、アプリコードを変更しない。
- ユーザー編集済みの画像、PDF、注釈、個人メモを上書き・削除・差し替えない。
- 委任やサブエージェント利用は、このファイルでは許可しない。実行環境の方針とユーザーの明示指示に従う。

## 完了

今回の依頼に対応する完了ゲートだけを適用する。無関係な全DB・全資料・全ブラウザ試験を機械的に追加しない。

保存・実装後は正本を再取得し、変更対象と影響範囲の不整合が0件であることを確認する。途中報告は終了を意味しない。安全に継続できる残作業がある場合は、定めた終了地点まで進める。
